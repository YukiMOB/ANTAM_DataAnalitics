# 二項検定の結果と比率グラフの関係

plot.negative_ratio_and_sigificant_difference <- function(ratio.value.list,bt.result){
  ratio.L <- data.frame(
    negative.ration = ratio.value.list,
    sigificant_difference = 
  )
}